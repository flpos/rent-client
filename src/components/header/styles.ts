import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
  text: {
    marginLeft: 'auto',
    marginRight: '28px',
  },
  button: {
    borderRadius: '50%',
    marginRight: '12px',
    height: '32px',
    width: '32px',
    minWidth: '32px',
    padding: '0px',
  },
});

export default useStyles;
